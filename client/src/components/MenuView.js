
const MenuView = () => {}

export default MenuView

// const { default: Axios } = require("axios");

// items,setitems=useState[]

// Axios.get(`api/menus/${id}`)
// (res.data.items)

// return with map of items

// in apps
// <Route exact path="/menu/:id" component={MenuView} />

//This is still in progress